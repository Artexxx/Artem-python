server {
    listen 80 default;
    location ^~ /uploads/ {
        root        /home/box/web/uploads;
    }
    location ~* ^.+\.\w+$ {
        root        /home/box/web/public;
    }
    location / {
        root        return 404;
    }
